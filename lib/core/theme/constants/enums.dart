enum ButtonType {
  primary,
  secondary,
  tertiary,
  outline,
}

enum AppThemeMode {
  light,
  dark,
  system,
}

enum Status {
  success,
  warning,
  error,
  info,
}

enum ProductType {
  physical,
  digital,
  subscription,
}

enum UserRole {
  admin,
  user,
  guest,
}

enum GridType {
  twoColumn,
  threeColumn,
  staggered,
}
